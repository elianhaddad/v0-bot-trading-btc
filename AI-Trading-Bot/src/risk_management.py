def apply_stop_loss(current_price, threshold):
    print(f"Aplicando stop loss: precio actual {current_price}, threshold {threshold}")
    # Aquí iría la lógica para calcular si se debe ejecutar un stop loss