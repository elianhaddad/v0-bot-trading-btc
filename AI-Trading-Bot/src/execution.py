def send_order_buy(symbol, quantity):
    print(f"Enviando orden de COMPRA para {symbol} de cantidad {quantity}")

def send_order_sell(symbol, quantity):
    print(f"Enviando orden de VENTA para {symbol} de cantidad {quantity}")